# Editable declaration of direct deps, ==-pinned to CI-green versions (same
# reproducibility logic as the ruff pin in CI). The fully-resolved lock -- this
# closure plus every transitive dep, pinned and hashed -- is requirements.lock,
# which is what CI installs. Regenerate it after any edit here with:
#   uv pip compile requirements.txt -o requirements.lock --universal --generate-hashes
yara-python==4.5.4
pytest==9.1.1
pyyaml==6.0.3
plyara==2.2.8   # parses rule source for the convention checks (tests/test_rule_conventions.py)
