# Direct deps ==-pinned to CI-green versions (same reproducibility logic as the
# ruff pin in CI); transitive deps still float -- a full lock file is on the roadmap.
yara-python==4.5.4
pytest==9.1.1
pyyaml==6.0.3
