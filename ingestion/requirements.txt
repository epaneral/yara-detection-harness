# Editable declaration of the ingestion component's direct runtime deps, ==-pinned to
# CI-green versions. They resolve, with the test runner, into requirements-dev.lock
# (every transitive dep pinned + hashed) -- the file CI installs. Regenerate after any
# edit with (command also in the lock header):
#   uv pip compile ingestion/requirements-dev.txt -o ingestion/requirements-dev.lock --universal --generate-hashes
httpx==0.28.1
# PR2 adds beautifulsoup4 here for the scraped-source adapter.
