__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/

# yaraQA rule-quality gate run artifacts (the tool is cloned in CI and writes these)
yaraqa-issues.json
yara-forge.log
/yaraqa-tool/

# Ingestion CLI output store (runtime artifact; fixtures are committed, the store is not)
ingestion/store/

# Secrets — never commit real API keys (enrichment-mcp)
.env
